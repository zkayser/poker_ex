// This is just a blank class that will hold the
// state of an ongoing game including the active table,
// the user, and the control components. The state
// should be initialized on the first message sent
// from the server after connecting on a channel.

export default class Store {
  
}